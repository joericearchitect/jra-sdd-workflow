# Issue and friction log

This is a concise, sanitized record of actionable problems encountered while
operating the workflow. It complements rather than replaces the authoritative
sources:

- GitHub Issues and Projects own product-work state.
- OpenSpec changes own requirements, design, and tasks.
- [Dogfood observations](../plans/dogfood-observations.md) own campaign-phase
  evidence.
- [Review records](../review-records/) own reviewed dispositions.

Do not record credentials, authentication codes, raw logs, or personal data.
Use repository-relative paths, check summaries, PR/issue numbers, and bounded
error descriptions as evidence. A note does not authorize a repair, a GitHub
mutation, cleanup, or automation.

## Automation evidence discipline

The campaign's eventual goal is to identify narrowly scoped automation that is
earned by manual evidence, not assumed from a design. For every issue or
workaround that could matter to automation, record the affected phase, manual
recovery, authorization and external-state boundaries, deterministic inputs and
outputs, safety gates, and known failure or restart behavior. Call out any
candidate follow-up change, but do not create it from the note alone.

Repeated, independently observed friction is evidence for a later Explore and
design review only after the campaign's stated manual-run threshold is met. The
resulting automation must preserve the existing human authorization, recovery,
and evidence gates; convenience is not a substitute for those controls.

## Current entries

| ID | Date | Status | Area | Summary | Exit or next action | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| IFL-001 | 2026-09-04 | Resolved | CI validation | The quoted validator-test glob in the GitHub Actions command was passed literally to Node 20, so the required tests were not found. | The workflow and contributor guidance now use shell-expanded test paths; retain direct CI coverage. | Archived [`align-tracking-contract` change](../../openspec/changes/archive/2026-09-04-align-tracking-contract/); its implementation and lifecycle PRs passed validation. |
| IFL-002 | 2026-09-04 | Resolved manual evidence | Post-Archive cleanup | The manual register, validation, audit, authorization, and receipt contract was delivered through the archived `formalize-workspace-cleanup` change. PF2 then completed the first qualifying manual cleanup run with a finished validated receipt and sanitized campaign observation. Earlier unregistered resources remain legacy and deliberately preserved. | Retain the manual contract for the remaining campaign runs. Revisit automation only after the campaign's repeated-run threshold and a separate design review; do not infer cleanup ownership for legacy resources. | [Cleanup workflow](../../docs/sdd-workflow.md#workspace-cleanup-after-lifecycle-delivery); [cleanup contract archive](../../openspec/changes/archive/2026-09-05-formalize-workspace-cleanup/); [PF2 observation](../plans/dogfood-observations.md#obs-005--pf2-completed-the-first-qualifying-manual-cleanup-run). |
| IFL-003 | 2026-09-04 | Operational constraint | Restricted execution environment | The sandbox could not update Git fetch/index metadata or access the authenticated GitHub CLI keyring. The same scoped operations succeeded through the host-authorized environment. | Run the same narrowly scoped GitHub/Git operation through an approved host environment; never replace or expose credentials merely because the sandbox cannot read them. | [Workflow authentication guidance](../../docs/sdd-workflow.md#prerequisites). |
| IFL-004 | 2026-09-04 | Open documentation gap | OpenSpec lifecycle adapters | The custom selected workflow has Sync and Verify assistant actions, but the generic `openspec instructions sync` and `openspec instructions verify` lookups are not available as artifacts. | Follow the selected assistant skill for Sync/Verify and use `openspec instructions specs` for the sync content rules. Evaluate a documentation clarification after repeated occurrences. | [Workflow selected actions](../../docs/sdd-workflow.md#selected-actions). |
| IFL-005 | 2026-09-04 | Resolved workaround | Runtime linkage validation | The reusable linkage validators could not read shell process-substitution descriptors in this environment (`EBADF`). | Use short-lived, sanitized temporary files for the PR body and changed-path list, then remove them after validation. Escalate to the reusable-skill owner only if this recurs or prevents a safe validation. | Lifecycle-record PR #4 validation evidence. |
| IFL-006 | 2026-09-05 | Open tooling defect | GitHub issue intake | The reusable `create-or-find-issue` helper failed before lookup with `ghCommand args must be an array of strings`, so the configured issue-authoring path could not create or reuse an issue. | Repair the reusable helper in its owning repository. Until then, use a host-authorized exact-title `gh issue list` check followed by one explicit, scoped `gh issue create`; do not infer duplicate absence. | [Issue #5](https://github.com/joericearchitect/jra-sdd-workflow/issues/5) was created through the constrained fallback. |
| IFL-007 | 2026-09-05 | Open follow-up change | GitHub issue intake configuration | PF2 could not use the configured issue-to-OpenSpec handoff because `config/sdd-github.json` is absent. After an exact-title search found no match, the owner authorized one scoped manual issue creation for [#8](https://github.com/joericearchitect/jra-sdd-workflow/issues/8); current Project evidence was then discovered before writing `tracking.yaml`. | Propose and deliver the repository-local intake configuration and documented manual create-or-reuse flow described in the design brief. Keep issue creation, Project changes, and plain Propose as separate authorization boundaries; assess later reusable-helper repair separately under IFL-006. | [Intake design brief](../design-briefs/github-intake-before-propose.md); [PF2 tracking](../../openspec/changes/archive/2026-09-05-align-artifact-quality-gates/tracking.yaml). |
| IFL-008 | 2026-09-05 | Resolved manual recovery | Implementation workspace registration | PF2's first secondary worktree used an administrative identity derived from its destination name, not the separately planned portable identity. The register accepted the branch but correctly blocked the worktree before any implementation edits. | After exact authorization, the clean mismatched worktree was removed and a second attempt named from the selected target was created, inspected, and registered. For future automation, treat worktree administrative identity as discovered evidence, not a value inferred from the change name; assess only after repeated manual occurrences. | Local PF2 workspace register; [workspace registration procedure](../../docs/sdd-workflow.md#register-local-delivery-resources-before-creation). |
| IFL-009 | 2026-09-05 | Open workflow handoff gap | Apply workspace setup | PF2 planning artifacts and the campaign log were uncommitted in the original worktree when its registered implementation worktree was created from the recorded default-branch commit. The new worktree therefore lacked its Apply context and required a manual, change-scoped copy while the original files were preserved. | In manual runs, copy and inspect only the selected change's planning artifacts and applicable campaign note before editing. For future automation, define an explicit, auditable planning-artifact handoff with exact source, destination, provenance, and verification; do not infer or copy unrelated original-worktree state. Assess after repeated manual occurrences. | Registered PF2 worktree attempt 2; [`align-artifact-quality-gates`](../../openspec/changes/archive/2026-09-05-align-artifact-quality-gates/) change context. |
| IFL-010 | 2026-09-05 | Resolved objective correction | Artifact evidence validation | PF2's first independent review proved that completed-task validation accepted a blank `Evidence:` field when a later heading followed because the whitespace match crossed a newline. This could allow automated gates to treat unevidenced completion as valid. | Evidence is now constrained to a non-blank value on the same line, with a focused regression probe that first reproduced and then rejected the false pass. Retain that probe; require design review before a second repair to this evidence classifier. | [`validate-openspec-artifacts.mjs`](../../scripts/validation/validate-openspec-artifacts.mjs); [`openspec-artifacts.test.mjs`](../../scripts/validation/test/openspec-artifacts.test.mjs). |
| IFL-011 | 2026-09-05 | Resolved manual workaround | Machine-readable command output | During PF2 Sync, the host login shell repeatedly prepended `Not privileged to set domain environment.` to successful OpenSpec, Node, and Git command output, including an OpenSpec JSON response. Manual inspection separated the warning from the command payload, but an adapter that assumes the complete shell stream is pure JSON could fail. | Run machine-readable commands without login-shell initialization when it is unnecessary; a non-login rerun produced clean output. Any later adapter must still reject invalid JSON rather than silently strip arbitrary diagnostics, and this single run does not justify new automation. | PF2 Sync command evidence; the same validation set passed without the warning under a non-login shell. |
| IFL-012 | 2026-09-05 | Resolved manual workaround | Archived tracking validation | After PF2 Archive, invoking the tracking validator with only the dated archive path inferred `2026-09-05-align-artifact-quality-gates` from the directory name and rejected the canonical `align-artifact-quality-gates` identifier. | Supply the validator's explicit `--change <canonical-change-name>` option for a dated archive path; the corrected invocation passed. Preserve the canonical identifier in `tracking.yaml`, and consider a documentation clarification only if this command-shape error recurs independently. | PF2 archived tracking validation; [`validate-tracking.mjs`](../../scripts/validation/validate-tracking.mjs). |
| IFL-013 | 2026-09-05 | Open workflow clarity gap | Gate 3 and cleanup authorization | Gate 3 approval correctly bound closure to a reviewed head, so an objective task-order correction required a renewed approval. Separately, the Gate 3 summary says closure covers exact cleanup, while the detailed cleanup procedure requires a new authorization after it displays exact eligible local targets. That wording led to a further explicit cleanup approval during the PF3 campaign-preflight recheck follow-up. | Keep the head-specific renewal and separate exact-target cleanup approval as the safe manual rule. In a later design review, reconcile the Gate 3 summary with the detailed cleanup boundary so the required approvals are predictable before delivery; do not weaken cleanup authorization or automate it from this single occurrence. | [`sdd-workflow.md`](../../docs/sdd-workflow.md) Gate 3 and workspace-cleanup guidance; [follow-up implementation PR #16](https://github.com/joericearchitect/jra-sdd-workflow/pull/16); [lifecycle PR #17](https://github.com/joericearchitect/jra-sdd-workflow/pull/17). |
| IFL-014 | 2026-09-07 | Observed manual evidence | Candidate 7 — reviewer, concurrent session | This reviewer session completed three planning-review passes for `resolve-pr-validation-signal`. Each targeted repair exposed a further implication: full pathway removal exposed workflow-input safety and task-order concerns; the safer repair exposed a missing alternatives rationale; review then reached shared campaign-plan state. The cause is not one weak implementer or reviewer. The repair process did not re-check the whole plan after each change, and three simultaneous sessions can change the shared campaign roadmap without a fixed evidence snapshot. The third pass also showed reviewer fallibility: it correctly identified that the roadmap rationale relied on stale local state, but incorrectly recommended candidate 4 as Done; `origin/main` records it In progress because delivery and machine-local cleanup remain incomplete. This entry is one reviewer observation for candidate 7, not evidence from the other concurrent sessions. | For current manual independent reviews, bind the prompt to one exact default-branch snapshot, selected change paths, and named shared-record diff; require a source-of-truth check that separates default-branch delivery evidence, current-worktree state, and machine-local cleanup evidence. After every repair, require a whole-plan sweep across proposal, design, tasks, tracking, issue scope, and the bound campaign record. For a future autonomous fix–test loop, make that bundle a deterministic input; run a fresh read-only reviewer after each objective fix; reject stale evidence; and return a second repair to design review. Do not automate from this one observation; compare the separately labelled observations from the other sessions first. | [Candidate 7 three-pass review](../scratch/2026-09-07-candidate-7-code-review.md); [`resolve-pr-validation-signal` planning artifacts](../../openspec/changes/resolve-pr-validation-signal/); [campaign roadmap](../plans/dogfood-10-changes.md). |

| IFL-015 | 2026-09-07 | Observed manual evidence | Candidate 5 — implementer, concurrent session | Candidate 5 needed three local planning-review passes, but the third pass did not contain six genuinely new defects: two findings repeated previously rejected warnings, one refined an unresolved coordination decision, two were shared-roadmap facts discovered late, and one was introduced by an earlier repair when a new baseline task was not connected to its dependent task. The implementer made repairs too locally and did not follow each edit with a whole-change consistency sweep. The reviewer also worked incrementally: it treated an unchanged commit as an unchanged review input even though untracked and shared files had changed, rediscovered visible roadmap inconsistencies late, repeated rejected findings because their dispositions existed only in chat, and reported Candidate 4 cleanup evidence as unavailable even though a completed machine-local receipt existed. Parallel work amplified both weaknesses because four active changes and their shared roadmap updates were present in one stale primary worktree instead of isolated candidate worktrees with serialized shared-record updates. Passing validators did not catch task-graph reachability, cross-record parity, contradictory rationale, whether requested evidence could actually be produced, or ownership of shared-file edits. | For the remaining manual reviews, bind each prompt to the exact candidate, worktree, base commit, changed and untracked file manifest, and content digest; name the shared-record snapshot and the authoritative source for delivery and cleanup facts; require a complete first-pass matrix across proposal, design, tasks, tracking, issue, and roadmap; and rerun that whole matrix after every repair rather than reviewing only touched lines. Classify findings as new, carried, repair-induced, pre-existing-but-late, or concurrent-state, and persist accepted, rejected, and human-decision dispositions in a reviewer-visible record. Review each candidate in an isolated worktree, then perform shared-roadmap reconciliation as a separate serialized integration step. For a future autonomous fix-test loop, use the same deterministic input bundle, invalidate review evidence whenever its digest changes, apply one bounded fix, rerun both affected and complete checks, and obtain a fresh read-only whole-change review. Serialize shared-file writes and return a component to design review when it needs a second repair. This observation records manual evidence only; it does not authorize automation and should be compared with the other concurrently labeled campaign observations. | [Candidate 5 three-pass review](../scratch/2026-09-07-candidate-5-code-review.md); [`add-docs-issue-template` planning artifacts](../../openspec/changes/add-docs-issue-template/); [campaign roadmap](../plans/dogfood-10-changes.md); [workspace cleanup guidance](../../docs/sdd-workflow.md#workspace-cleanup-after-lifecycle-delivery) |
| IFL-016 | 2026-09-07 | Observed manual evidence | Candidate 8 — planning-artifact implementer, concurrent session | Candidate 8 received three planning-review passes. Pass 1 found that the initial parser-focused plan omitted two real consumers, their recovery behavior, fixture-portability coverage, and a Batch 2 readiness conflict. Pass 2 followed repairs that widened scope but had not rechecked the whole system: the linkage test had no owner or declared path, the new candidate-7 dependency was absent from the roadmap, and some contract wording described behavior the validator does not expose. Pass 3 found only internal consistency drift: one proposal line still named one of two delta specs, the design did not repeat the candidate-7 test-harness decision, and task metadata mixed an internal task ID with a cross-change delivery gate. The implementer repaired reported lines rather than first rebuilding one complete map from parser consumer, through behavior and recovery, to source edit, test owner, delta spec, task, tracking path, and roadmap dependency. The reviewer usefully found that drift, but it also reviewed successive local patches rather than requiring that complete map after each repair. Parallel sessions amplified the issue: Candidate 8 was planned while its Batch 1 dependency, Candidate 7, still owned an unmerged linkage-test file and the shared roadmap was changing. Passing validators proved artifact shape and existing behavior, not that all planning records described the same complete scope. | For current manual independent reviews, give the reviewer an exact snapshot: selected change, base commit, changed and untracked manifest, candidate artifact set, named shared-roadmap snapshot, and source of truth for dependency and delivery state. Require an initial consumer-to-coverage matrix with one row per affected path: behavior, recovery, source edit or no-edit assertion, test file and owner, specification, task, tracking path, and cross-change dependency. After every accepted repair, rerun that whole matrix across proposal, design, specs, tasks, tracking, issue scope, and roadmap; do not review only the changed lines. Record each finding as new, repair-induced, carried, pre-existing-but-late, or concurrent-state, and persist its disposition so later reviewers do not rediscover or reverse it. For a future autonomous fix-test loop, make that snapshot and matrix deterministic inputs; invalidate prior review evidence when their digest changes; permit one bounded fix, affected and complete tests, then a fresh read-only whole-change review; serialize shared-record writes; and return a component to design review on a second repair. This is one implementer observation from Candidate 8, not a conclusion about the other sessions and not authorization to automate; compare it with the separately labeled Candidate 5 and Candidate 7 observations. | [Candidate 8 three-pass review](../scratch/2026-09-07-candidate-8-code-review.md); [`add-tracking-schema-examples` planning artifacts](../../openspec/changes/add-tracking-schema-examples/); [campaign roadmap](../plans/dogfood-10-changes.md); [Candidate 5 observation](#current-entries); [Candidate 7 observation](#current-entries) |

## Entry template

Add an entry when a problem is reproducible, blocks a phase, requires a
workaround, or supplies evidence for a future workflow decision. Link the
authoritative artifact instead of copying large content.

| ID | Date | Status | Area | Summary | Exit or next action | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| IFL-XXX | YYYY-MM-DD | Open / Resolved / Operational constraint | | | | |

## Review cadence

- Update an entry's status when its stated exit has durable evidence.
- Move campaign-specific recurring friction into the campaign observation
  ledger; add or update a review record when a human makes a material
  disposition.
- Treat repeated occurrences as input to an Explore decision, not as automatic
  authority to build a controller or cleanup automation.
